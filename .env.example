# =============================================================================
# SMART_TEST (webtest) - Environment Variables Template
#
# 使用说明:
#   1. 复制此文件为 .env: cp .env.example .env
#   2. 修改 .env 中的敏感配置（密码、密钥等）
#   3. 确保 .env 文件不会被提交到版本控制
# =============================================================================

# -----------------------------------------------------------------------------
# 数据库配置
# -----------------------------------------------------------------------------
# 数据库类型: sqlite (开发) / postgres (生产)
DB_TYPE=postgres

# PostgreSQL 连接配置
# 开发环境使用 localhost，生产环境使用 Docker 服务名 postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=webtest
DB_PASSWORD=webtest_default_pass_change_me
DB_NAME=webtest

# 连接池配置
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10

# -----------------------------------------------------------------------------
# 应用配置
# -----------------------------------------------------------------------------
# JWT 签名密钥 (请使用强随机字符串)
JWT_SECRET=default_jwt_secret_please_change_in_production

# 日志级别: debug / info / warn / error
LOG_LEVEL=info

# 时区
TZ=Asia/Shanghai

# -----------------------------------------------------------------------------
# MCP 服务配置
# -----------------------------------------------------------------------------
# MCP 认证令牌
MCP_AUTH_TOKEN=default_mcp_token_change_me

# MCP 后端服务地址
MCP_BACKEND_URL=https://localhost:8443

# MCP 服务模式: stdio | http
MCP_SERVER_MODE=http

# MCP HTTP 监听地址
MCP_HTTP_ADDR=:16410

# -----------------------------------------------------------------------------
# Playwright 配置
# -----------------------------------------------------------------------------
# Playwright Executor URL
# 开发环境使用 localhost:53730，生产环境使用 http://playwright-executor:53730
PLAYWRIGHT_EXECUTOR_URL=http://localhost:53730

# -----------------------------------------------------------------------------
# HTTPS 证书配置
# -----------------------------------------------------------------------------
# 证书文件路径
# 开发环境使用相对路径，生产环境容器内使用绝对路径 /app/certs/
CERT_FILE=certs/server.crt
KEY_FILE=certs/server.key

# -----------------------------------------------------------------------------
# PostgreSQL 容器配置 (用于 docker-compose)
# -----------------------------------------------------------------------------
POSTGRES_DB=webtest
POSTGRES_USER=webtest
# 注意: POSTGRES_PASSWORD 应与 DB_PASSWORD 保持一致
POSTGRES_PASSWORD=webtest_default_pass_change_me
